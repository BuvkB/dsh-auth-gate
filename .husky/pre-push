#!/usr/bin/env sh
# Pre-push: run the full type-check so push-blocking type errors surface
# locally instead of in CI. Pre-commit stays cheap (lint-staged only); the
# slower full project check moves here, where it runs at most once per push
# and benefits from tsbuildinfo caching after the first run.
npm run type-check
